@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Monthly Bills";
}

<div class="container-fluid">
    <!-- Toaster Container -->
    <div id="toaster" class="position-fixed top-0 end-0 p-3" style="z-index: 1050;"></div>

    <!-- Page Header -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-gray-800 fw-normal"><i class="bi bi-file-earmark-text me-2 text-success"></i>Monthly Bills</h2>
        <div>
            <button class="btn btn-outline-success me-2" data-bs-toggle="modal" data-bs-target="#billModal" onclick="prepareAddBill()">
                <i class="bi bi-plus-circle me-1"></i> Add Bill
            </button>
            <button class="btn btn-outline-secondary" onclick="showBillGenerationSection()">
                <i class="bi bi-lightning-charge me-1"></i> Generate Bills
            </button>
        </div>
    </div>

    <!-- Bill Generation Section -->
    <div id="billGenerationSection" class="card border-0 shadow-sm mb-4 bg-gray-50" style="display: none;">
        <div class="card-header bg-transparent border-0 py-3">
            <h5 class="mb-0 text-gray-700"><i class="bi bi-gear me-2 text-success"></i>Generate Monthly Bills</h5>
        </div>
        <div class="card-body">
            <div class="row g-3 align-items-end">
                <div class="col-md-3">
                    <label for="genMonth" class="form-label text-gray-700">Month</label>
                    <select class="form-select border-gray-300" id="genMonth">
                        <option value="1">January</option>
                        <option value="2">February</option>
                        <option value="3">March</option>
                        <option value="4">April</option>
                        <option value="5">May</option>
                        <option value="6">June</option>
                        <option value="7">July</option>
                        <option value="8">August</option>
                        <option value="9">September</option>
                        <option value="10">October</option>
                        <option value="11">November</option>
                        <option value="12">December</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label for="genYear" class="form-label text-gray-700">Year</label>
                    <input type="number" class="form-control border-gray-300" id="genYear" min="2000" max="2100" value="@DateTime.Now.Year">
                </div>
                <div class="col-md-6">
                    <button class="btn btn-success" onclick="generateBillsFromReadings()">
                        <i class="bi bi-calculator me-1"></i> Generate from Daily Readings
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Generate Bill for Specific Meter Section -->
    <div class="card border-0 shadow-sm mb-4 bg-gray-50">
        <div class="card-header bg-success border-0 py-3">
            <h5 class="mb-0 text-gray-900"><i class="bi bi-speedometer2 me-2 text-gray-900"></i>Generate Bill for Specific Meter</h5>
        </div>
        <div class="card-body">
            <div class="row g-3 align-items-end">
                <div class="col-md-3">
                    <label for="singleMeter" class="form-label text-gray-700">Select Meter</label>
                    <select class="form-select border-gray-300" id="singleMeter">
                        <option value="">-- Select Meter --</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label for="singleMeterMonth" class="form-label text-gray-700">Month</label>
                    <select class="form-select border-gray-300" id="singleMeterMonth">
                        <option value="1">January</option>
                        <option value="2">February</option>
                        <option value="3">March</option>
                        <option value="4">April</option>
                        <option value="5">May</option>
                        <option value="6">June</option>
                        <option value="7">July</option>
                        <option value="8">August</option>
                        <option value="9">September</option>
                        <option value="10">October</option>
                        <option value="11">November</option>
                        <option value="12">December</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label for="singleMeterYear" class="form-label text-gray-700">Year</label>
                    <input type="number" class="form-control border-gray-300" id="singleMeterYear" min="2000" max="2100">
                </div>
                <div class="col-md-3">
                    <button class="btn btn-outline-success w-100" onclick="generateBillForMeter()">
                        <i class="bi bi-file-earmark-plus me-1"></i> Generate Bill
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Advanced Filters Section -->
    <div class="card border-0 shadow-sm mb-4 bg-gray-50">
        <div class="card-header bg-success border-0 py-3">
            <h5 class="mb-0 text-gray-900"><i class="bi bi-funnel me-2 text-gray-900"></i>Filter Bills</h5>
        </div>
        <div class="card-body">
            <div class="row g-3 align-items-end">
                <div class="col-md-2">
                    <label for="filterMonth" class="form-label text-gray-700">Month</label>
                    <select class="form-select border-gray-300" id="filterMonth">
                        <option value="0">All Months</option>
                        <option value="1">January</option>
                        <option value="2">February</option>
                        <option value="3">March</option>
                        <option value="4">April</option>
                        <option value="5">May</option>
                        <option value="6">June</option>
                        <option value="7">July</option>
                        <option value="8">August</option>
                        <option value="9">September</option>
                        <option value="10">October</option>
                        <option value="11">November</option>
                        <option value="12">December</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label for="filterYear" class="form-label text-gray-700">Year</label>
                    <input type="number" class="form-control border-gray-300" id="filterYear" min="2000" max="2100" value="@DateTime.Now.Year">
                </div>
                <div class="col-md-2">
                    <label for="filterConsumer" class="form-label text-gray-700">Consumer</label>
                    <select class="form-select border-gray-300" id="filterConsumer">
                        <option value="0">All Consumers</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label for="filterMeter" class="form-label text-gray-700">Meter</label>
                    <select class="form-select border-gray-300" id="filterMeter">
                        <option value="0">All Meters</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label for="filterStatus" class="form-label text-gray-700">Status</label>
                    <select class="form-select border-gray-300" id="filterStatus">
                        <option value="">All Status</option>
                        <option value="Pending">Pending</option>
                        <option value="Paid">Paid</option>
                        <option value="Overdue">Overdue</option>
                        <option value="Cancelled">Cancelled</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <label for="filterAmountRange" class="form-label text-gray-700">Amount Range</label>
                    <select class="form-select border-gray-300" id="filterAmountRange">
                        <option value="0">All Amounts</option>
                        <option value="1">0 - 500</option>
                        <option value="2">501 - 1000</option>
                        <option value="3">1001 - 2000</option>
                        <option value="4">2000+</option>
                    </select>
                </div>
            </div>
            <div class="row mt-3">
                <div class="col-md-12">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <button class="btn btn-outline-secondary me-2" onclick="applyFilters()">
                                <i class="bi bi-funnel me-1"></i> Apply Filters
                            </button>
                            <button class="btn btn-outline-secondary" onclick="clearFilters()">
                                <i class="bi bi-x-circle me-1"></i> Clear Filters
                            </button>
                        </div>
                        <div class="text-gray-700">
                            <span id="filteredCount">0</span> bills found |
                            Total: ₹<span id="filteredAmount">0.00</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Loading Indicator -->
    <div id="loadingIndicator" class="text-center my-4" style="display: none;">
        <div class="spinner-border text-success" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
        <p class="mt-2 text-gray-600">Loading bills...</p>
    </div>

    <!-- Main Bills Table -->
    <div class="card border-0 shadow-sm">
        <div class="card-header bg-success border-0 py-3">
            <div class="d-flex justify-content-between align-items-center">
                <h5 class="mb-0 text-gray-900"><i class="bi bi-list me-2 text-gray-900"></i>All Monthly Bills</h5>
                <div class="text-gray-900">
                    <small>
                        Total: <span id="totalBillsCount">0</span> bills |
                        Amount: ₹<span id="totalBillsAmount">0.00</span> |
                        Consumption: <span id="totalBillsConsumption">0.00</span> kWh
                    </small>
                </div>
            </div>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="bg-gray-200 text-gray-800">
                        <tr>
                            <th class="border-0">Bill ID</th>
                            <th class="border-0">Consumer</th>
                            <th class="border-0">Meter</th>
                            <th class="border-0">Period</th>
                            <th class="border-0">Billing Date</th>
                            <th class="border-0">Consumption (kWh)</th>
                            <th class="border-0">Amount (₹)</th>
                            <th class="border-0">Status</th>
                            <th class="border-0">Actions</th>
                        </tr>
                    </thead>
                    <tbody id="billsTableBody">
                        <tr>
                            <td colspan="9" class="text-center text-gray-600 py-4">Loading bills...</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- Add/Edit Modal -->
<div class="modal fade" id="billModal" tabindex="-1" aria-labelledby="billModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content border-0 shadow">
            <div class="modal-header bg-gray-100 border-0">
                <h5 class="modal-title text-gray-800" id="billModalLabel">Add Monthly Bill</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="billForm">
                    <input type="hidden" id="isEdit" value="false" />
                    <input type="hidden" id="billId" value="0" />

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="consumerId" class="form-label text-gray-700">Consumer <span class="text-success">*</span></label>
                            <select class="form-select border-gray-300" id="consumerId" required onchange="updateMetersByConsumer()">
                                <option value="">-- Select Consumer --</option>
                            </select>
                            <div class="form-text text-gray-600" id="consumerInfo"></div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="meterId" class="form-label text-gray-700">Meter <span class="text-success">*</span></label>
                            <select class="form-select border-gray-300" id="meterId" required>
                                <option value="">-- Select Meter --</option>
                            </select>
                            <div class="form-text text-gray-600" id="meterInfo"></div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-4 mb-3">
                            <label for="billingMonth" class="form-label text-gray-700">Billing Month <span class="text-success">*</span></label>
                            <select class="form-select border-gray-300" id="billingMonth" required>
                                <option value="">-- Select Month --</option>
                                <option value="1">January</option>
                                <option value="2">February</option>
                                <option value="3">March</option>
                                <option value="4">April</option>
                                <option value="5">May</option>
                                <option value="6">June</option>
                                <option value="7">July</option>
                                <option value="8">August</option>
                                <option value="9">September</option>
                                <option value="10">October</option>
                                <option value="11">November</option>
                                <option value="12">December</option>
                            </select>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label for="billingYear" class="form-label text-gray-700">Billing Year <span class="text-success">*</span></label>
                            <input type="number" class="form-control border-gray-300" id="billingYear" min="2000" max="2100" required>
                        </div>
                        <div class="col-md-4 mb-3">
                            <label for="billingDate" class="form-label text-gray-700">Billing Date <span class="text-success">*</span></label>
                            <input type="date" class="form-control border-gray-300" id="billingDate" required>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="totalConsumptionKwh" class="form-label text-gray-700">Total Consumption (kWh) <span class="text-success">*</span></label>
                            <input type="number" step="0.01" class="form-control border-gray-300" id="totalConsumptionKwh" required
                                   onchange="calculateEstimatedAmount()">
                            <div class="form-text text-gray-600">Based on consumption and rate</div>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="totalAmount" class="form-label text-gray-700">Total Amount (₹) <span class="text-success">*</span></label>
                            <input type="number" step="0.01" class="form-control border-gray-300" id="totalAmount" required>
                            <div class="form-text text-gray-600" id="amountInfo"></div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-12 mb-3">
                            <label for="billStatus" class="form-label text-gray-700">Bill Status <span class="text-success">*</span></label>
                            <select class="form-select border-gray-300" id="billStatus" required>
                                <option value="">-- Select Status --</option>
                                <option value="Pending">Pending</option>
                                <option value="Paid">Paid</option>
                                <option value="Overdue">Overdue</option>
                                <option value="Cancelled">Cancelled</option>
                            </select>
                        </div>
                    </div>
                </form>
            </div>
            <div class="modal-footer bg-gray-100 border-0">
                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-success" onclick="saveBill()">
                    <i class="bi bi-save me-1"></i> Save Bill
                </button>
            </div>
        </div>
    </div>
</div>

<style>
    /* Custom gray tones with green highlights */
    .bg-gray-50 { background-color: #f9fafb; }
    .bg-gray-100 { background-color: #f3f4f6; }
    .bg-gray-200 { background-color: #e5e7eb; }
    .text-gray-600 { color: #6b7280; }
    .text-gray-700 { color: #374151; }
    .text-gray-800 { color: #1f2937; }
    .border-gray-300 { border-color: #d1d5db; }
    
    /* Table styling */
    .table-hover tbody tr:hover {
        background-color: #f8f9fa;
    }
    
    /* Toaster styling */
    .toast-item {
        min-width: 300px;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        border-radius: 0.375rem;
        overflow: hidden;
        margin-bottom: 0.75rem;
    }
    
    .toast-success {
        background-color: #10b981;
        color: white;
    }
    
    .toast-error {
        background-color: #ef4444;
        color: white;
    }
</style>

@section Scripts {
    <script>
        // Configuration
        const BILL_API_URL = `${API_BASE_URL}/api/MonthlyBill`;
        const CONSUMER_API_URL = `${API_BASE_URL}/api/Consumer`;
        const METER_API_URL = `${API_BASE_URL}/api/Meter`;

        // Global variables
        let allBills = [];
        let allConsumers = [];
        let allMeters = [];
        let filteredBills = [];
        let billModal;

        // Initialize when page loads
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize modals
            billModal = new bootstrap.Modal(document.getElementById('billModal'));

            // Set current month/year
            const currentDate = new Date();
            const currentMonth = currentDate.getMonth() + 1;
            const currentYear = currentDate.getFullYear();

            document.getElementById('genMonth').value = currentMonth;
            document.getElementById('billingMonth').value = currentMonth;
            document.getElementById('billingYear').value = currentYear;
            document.getElementById('genYear').value = currentYear;
            document.getElementById('filterYear').value = currentYear;
            document.getElementById('filterMonth').value = currentMonth;
            document.getElementById('singleMeterMonth').value = currentMonth;
            document.getElementById('singleMeterYear').value = currentYear;

            // Set default billing date to today
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('billingDate').value = today;

            // Load initial data
            fetchBills();
            fetchConsumers();
            fetchMeters();

            // Initialize filters
            initializeFilters();
        });

        // Toaster function
        function showToast(message, type = 'success') {
            const toaster = document.getElementById('toaster');
            const toastId = 'toast-' + Date.now();
            
            const toastHtml = `
                <div id="${toastId}" class="toast-item toast-${type}">
                    <div class="d-flex justify-content-between align-items-center p-3">
                        <div class="d-flex align-items-center">
                            <i class="bi ${type === 'success' ? 'bi-check-circle' : 'bi-exclamation-triangle'} me-2"></i>
                            <span>${message}</span>
                        </div>
                        <button type="button" class="btn-close btn-close-white" onclick="document.getElementById('${toastId}').remove()"></button>
                    </div>
                </div>
            `;
            
            toaster.insertAdjacentHTML('beforeend', toastHtml);
            
            // Auto remove after 3 seconds
            setTimeout(() => {
                const toastElement = document.getElementById(toastId);
                if (toastElement) {
                    toastElement.remove();
                }
            }, 3000);
        }

        function showError(message) {
            showToast(message, 'error');
        }

        function showSuccess(message) {
            showToast(message, 'success');
        }

        function initializeFilters() {
            document.getElementById('filterMonth').addEventListener('change', applyFilters);
            document.getElementById('filterYear').addEventListener('change', applyFilters);
            document.getElementById('filterConsumer').addEventListener('change', applyFilters);
            document.getElementById('filterMeter').addEventListener('change', applyFilters);
            document.getElementById('filterStatus').addEventListener('change', applyFilters);
            document.getElementById('filterAmountRange').addEventListener('change', applyFilters);
        }

        function showBillGenerationSection() {
            const section = document.getElementById('billGenerationSection');
            section.style.display = section.style.display === 'none' ? 'block' : 'none';
        }

        async function generateBillsFromReadings() {
            const month = parseInt(document.getElementById('genMonth').value);
            const year = parseInt(document.getElementById('genYear').value);

            if (!month || !year) {
                showError('Please select both month and year');
                return;
            }

            if (!confirm(`Generate monthly bills from daily readings for ${month}/${year}?`)) {
                return;
            }

            try {
                showLoading(true);
                const response = await fetch(`${BILL_API_URL}/GenerateBillsFromReadings`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ month, year }),
                });

                if (response.ok) {
                    const result = await response.json();
                    showSuccess(result.message || `Bills generated successfully for ${month}/${year}`);
                    await fetchBills();
                } else {
                    const errorText = await response.text();
                    throw new Error(errorText);
                }
            } catch (error) {
                console.error('Error generating bills from readings:', error);
                showError('Failed to generate bills: ' + error.message);
            } finally {
                showLoading(false);
            }
        }

        async function fetchBills() {
            try {
                showLoading(true);
                const response = await fetch(`${BILL_API_URL}/AllMonthlyBills`);

                if (response.ok) {
                    allBills = await response.json();
                    filteredBills = [...allBills];
                    renderBillsTable(filteredBills);
                    updateFilterStats();
                } else {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
            } catch (error) {
                console.error('Error loading bills:', error);
                showError('Failed to load bills: ' + error.message);
            } finally {
                showLoading(false);
            }
        }

        async function fetchConsumers() {
            try {
                const response = await fetch(`${CONSUMER_API_URL}/AllConsumers`);

                if (response.ok) {
                    allConsumers = await response.json();
                    populateConsumersDropdown();
                    populateFilterConsumers();
                } else {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
            } catch (error) {
                console.error('Error loading consumers:', error);
            }
        }

        async function fetchMeters() {
            try {
                const response = await fetch(`${METER_API_URL}/AllMeters`);

                if (response.ok) {
                    allMeters = await response.json();
                    populateMetersDropdown();
                    populateFilterMeters();
                    populateSingleMeterDropdown();
                } else {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
            } catch (error) {
                console.error('Error loading meters:', error);
            }
        }

        // Enhanced filtering system
        function applyFilters() {
            const month = parseInt(document.getElementById('filterMonth').value) || 0;
            const year = parseInt(document.getElementById('filterYear').value) || 0;
            const consumerId = parseInt(document.getElementById('filterConsumer').value) || 0;
            const meterId = parseInt(document.getElementById('filterMeter').value) || 0;
            const status = document.getElementById('filterStatus').value;
            const amountRange = parseInt(document.getElementById('filterAmountRange').value) || 0;

            filteredBills = allBills.filter(bill => {
                if (month > 0 && bill.billingMonth !== month) return false;
                if (year > 0 && bill.billingYear !== year) return false;
                if (consumerId > 0 && bill.consumerId !== consumerId) return false;
                if (meterId > 0 && bill.meterId !== meterId) return false;
                if (status && bill.billStatus !== status) return false;

                if (amountRange > 0) {
                    switch(amountRange) {
                        case 1: if (bill.totalAmount > 500) return false; break;
                        case 2: if (bill.totalAmount < 501 || bill.totalAmount > 1000) return false; break;
                        case 3: if (bill.totalAmount < 1001 || bill.totalAmount > 2000) return false; break;
                        case 4: if (bill.totalAmount <= 2000) return false; break;
                    }
                }

                return true;
            });

            renderBillsTable(filteredBills);
            updateFilterStats();
        }

        function clearFilters() {
            document.getElementById('filterMonth').value = '0';
            document.getElementById('filterYear').value = new Date().getFullYear();
            document.getElementById('filterConsumer').value = '0';
            document.getElementById('filterMeter').value = '0';
            document.getElementById('filterStatus').value = '';
            document.getElementById('filterAmountRange').value = '0';

            filteredBills = [...allBills];
            renderBillsTable(filteredBills);
            updateFilterStats();
        }

        function updateFilterStats() {
            const totalAmount = filteredBills.reduce((sum, bill) => sum + (bill.totalAmount || 0), 0);
            const totalConsumption = filteredBills.reduce((sum, bill) => sum + (bill.totalConsumptionKwh || 0), 0);

            document.getElementById('filteredCount').textContent = filteredBills.length;
            document.getElementById('filteredAmount').textContent = totalAmount.toFixed(2);
            document.getElementById('totalBillsCount').textContent = filteredBills.length;
            document.getElementById('totalBillsAmount').textContent = totalAmount.toFixed(2);
            document.getElementById('totalBillsConsumption').textContent = totalConsumption.toFixed(2);
        }

        function populateConsumersDropdown() {
            const select = document.getElementById('consumerId');
            select.innerHTML = '<option value="">-- Select Consumer --</option>';
            allConsumers.forEach(consumer => {
                select.innerHTML += `<option value="${consumer.consumerId}">${consumer.name || 'Consumer ' + consumer.consumerId}</option>`;
            });
        }

        function populateFilterConsumers() {
            const select = document.getElementById('filterConsumer');
            select.innerHTML = '<option value="0">All Consumers</option>';
            allConsumers.forEach(consumer => {
                select.innerHTML += `<option value="${consumer.consumerId}">${consumer.name}</option>`;
            });
        }

        function populateMetersDropdown() {
            const select = document.getElementById('meterId');
            select.innerHTML = '<option value="">-- Select Meter --</option>';
            allMeters.forEach(meter => {
                select.innerHTML += `<option value="${meter.meterId}">Meter ${meter.meterId}</option>`;
            });
        }

        function populateFilterMeters() {
            const select = document.getElementById('filterMeter');
            select.innerHTML = '<option value="0">All Meters</option>';
            allMeters.forEach(meter => {
                select.innerHTML += `<option value="${meter.meterId}">Meter ${meter.meterId}</option>`;
            });
        }

        function populateSingleMeterDropdown() {
            const select = document.getElementById('singleMeter');
            select.innerHTML = '<option value="">-- Select Meter --</option>';
            allMeters.forEach(meter => {
                const consumer = allConsumers.find(c => c.consumerId === meter.consumerId);
                const consumerName = consumer ? consumer.name : 'Unknown';
                select.innerHTML += `<option value="${meter.meterId}">Meter ${meter.meterId} - ${consumerName}</option>`;
            });
        }

        function updateMetersByConsumer() {
            const consumerId = parseInt(document.getElementById('consumerId').value);
            const meterSelect = document.getElementById('meterId');

            meterSelect.innerHTML = '<option value="">-- Select Meter --</option>';

            if (consumerId) {
                const consumerMeters = allMeters.filter(meter => meter.consumerId === consumerId);
                consumerMeters.forEach(meter => {
                    meterSelect.innerHTML += `<option value="${meter.meterId}">Meter ${meter.meterId}</option>`;
                });
            }
        }

        function renderBillsTable(bills) {
            const tbody = document.getElementById('billsTableBody');

            if (!tbody) {
                console.error('Bills table body not found!');
                return;
            }

            if (bills && bills.length > 0) {
                let tableHtml = '';
                bills.forEach(bill => {
                    const billingDate = new Date(bill.billingDate).toLocaleDateString('en-IN');
                    const period = bill.billingMonth + '/' + bill.billingYear;

                    let statusBadge;
                    switch(bill.billStatus) {
                        case 'Paid':
                            statusBadge = '<span class="badge bg-success">Paid</span>';
                            break;
                        case 'Overdue':
                            statusBadge = '<span class="badge bg-danger">Overdue</span>';
                            break;
                        case 'Cancelled':
                            statusBadge = '<span class="badge bg-secondary">Cancelled</span>';
                            break;
                        default:
                            statusBadge = '<span class="badge bg-warning text-dark">Pending</span>';
                    }

                    tableHtml += `
                        <tr>
                            <td class="text-gray-700"><strong>${bill.billId}</strong></td>
                            <td class="text-gray-700">${bill.consumerName || 'N/A'}</td>
                            <td class="text-gray-700">Meter #${bill.meterId}</td>
                            <td class="text-gray-700">${period}</td>
                            <td class="text-gray-700">${billingDate}</td>
                            <td class="text-gray-700">${(bill.totalConsumptionKwh || 0).toFixed(2)}</td>
                            <td class="text-gray-700"><strong>₹${(bill.totalAmount || 0).toFixed(2)}</strong></td>
                            <td>${statusBadge}</td>
                            <td>
                                <button class="btn btn-sm btn-outline-secondary me-1" onclick="viewBillDetails(${bill.billId})" title="View Details">
                                    <i class="bi bi-eye"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-secondary me-1" onclick="editBill(${bill.billId})" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-secondary" onclick="deleteBill(${bill.billId})" title="Delete">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    `;
                });
                tbody.innerHTML = tableHtml;
            } else {
                tbody.innerHTML = '<tr><td colspan="9" class="text-center text-gray-600 py-4">No bills found matching the criteria.</td></tr>';
            }
        }

        async function generateBillForMeter() {
            const meterId = parseInt(document.getElementById('singleMeter').value);
            const month = parseInt(document.getElementById('singleMeterMonth').value);
            const year = parseInt(document.getElementById('singleMeterYear').value);

            if (!meterId) {
                showError('Please select a meter');
                return;
            }

            if (!month || !year) {
                showError('Please select both month and year');
                return;
            }

            const meter = allMeters.find(m => m.meterId === meterId);
            const consumer = allConsumers.find(c => c.consumerId === meter?.consumerId);
            const monthName = new Date(year, month - 1, 1).toLocaleDateString('en-US', { month: 'long' });

            if (!confirm(`Generate bill for Meter #${meterId} (${consumer?.name || 'Unknown'}) for ${monthName} ${year}?`)) {
                return;
            }

            try {
                showLoading(true);
                const response = await fetch(`${BILL_API_URL}/GenerateBillForMeter`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ meterId, month, year }),
                });

                if (response.ok) {
                    const result = await response.json();

                    if (result.billsGenerated > 0) {
                        showSuccess(result.message);
                        await fetchBills();

                        // Clear the form
                        document.getElementById('singleMeter').value = '';
                    } else {
                        showError(result.message);
                    }
                } else {
                    const errorData = await response.json();
                    throw new Error(errorData.error || 'Failed to generate bill');
                }
            } catch (error) {
                console.error('Error generating bill for meter:', error);
                showError(error.message || 'Failed to generate bill for meter');
            } finally {
                showLoading(false);
            }
        }

        function prepareAddBill() {
            document.getElementById('billModalLabel').textContent = 'Add Monthly Bill';
            document.getElementById('billForm').reset();
            document.getElementById('isEdit').value = 'false';
            document.getElementById('billId').value = '0';

            const currentDate = new Date();
            document.getElementById('billingMonth').value = currentDate.getMonth() + 1;
            document.getElementById('billingYear').value = currentDate.getFullYear();
            document.getElementById('billingDate').value = currentDate.toISOString().split('T')[0];
        }

        async function editBill(billId) {
            const bill = allBills.find(b => b.billId === billId);
            if (!bill) {
                showError('Bill not found');
                return;
            }

            document.getElementById('billModalLabel').textContent = 'Edit Monthly Bill';
            document.getElementById('isEdit').value = 'true';
            document.getElementById('billId').value = bill.billId;
            document.getElementById('consumerId').value = bill.consumerId;
            document.getElementById('meterId').value = bill.meterId;
            document.getElementById('billingDate').value = bill.billingDate;
            document.getElementById('billingMonth').value = bill.billingMonth;
            document.getElementById('billingYear').value = bill.billingYear;
            document.getElementById('totalConsumptionKwh').value = bill.totalConsumptionKwh;
            document.getElementById('totalAmount').value = bill.totalAmount;
            document.getElementById('billStatus').value = bill.billStatus;

            billModal.show();
        }

        function calculateEstimatedAmount() {
            const consumption = parseFloat(document.getElementById('totalConsumptionKwh').value) || 0;
            const baseRate = 7.5;

            if (consumption > 0) {
                const estimatedAmount = consumption * baseRate;
                document.getElementById('totalAmount').value = estimatedAmount.toFixed(2);
                document.getElementById('amountInfo').textContent = `Estimated based on ₹${baseRate} per kWh`;
            }
        }

         async function saveBill() {
            const isEdit = document.getElementById('isEdit').value === 'true';
            const billId = parseInt(document.getElementById('billId').value);
            const consumerId = parseInt(document.getElementById('consumerId').value);
            const meterId = parseInt(document.getElementById('meterId').value);
            const billingDate = document.getElementById('billingDate').value;
            const billingMonth = parseInt(document.getElementById('billingMonth').value);
            const billingYear = parseInt(document.getElementById('billingYear').value);
            const totalConsumptionKwh = parseFloat(document.getElementById('totalConsumptionKwh').value);
            const totalAmount = parseFloat(document.getElementById('totalAmount').value);
            const billStatus = document.getElementById('billStatus').value;

            if (!consumerId || !meterId || !billingDate || !billingMonth || !billingYear || !totalConsumptionKwh || !totalAmount || !billStatus) {
                showError('Please fill all required fields');
                return;
            }

            if (totalConsumptionKwh < 0 || totalAmount < 0) {
                showError('Consumption and amount must be positive values');
                return;
            }

            const billData = {
                billId: isEdit ? billId : null,
                consumerId: consumerId,
                meterId: meterId,
                billingDate: billingDate,
                billingMonth: billingMonth,
                billingYear: billingYear,
                totalConsumptionKwh: totalConsumptionKwh,
                totalAmount: totalAmount,
                billStatus: billStatus
            };

            const endpoint = isEdit ? `${BILL_API_URL}/Update` : `${BILL_API_URL}/Create`;
            const method = isEdit ? 'PUT' : 'POST';

            try {
                showLoading(true);
                const response = await fetch(endpoint, {
                    method: method,
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(billData),
                });

                if (response.ok) {
                    billModal.hide();
                    showSuccess(isEdit ? 'Bill updated successfully!' : 'Bill created successfully!');
                    await fetchBills();
                } else {
                    const errorData = await response.json();
                    throw new Error(errorData.error || 'Failed to save bill');
                }
            } catch (error) {
                console.error('Error saving bill:', error);
                showError('Failed to save bill: ' + error.message);
            } finally {
                showLoading(false);
            }
        }

        async function deleteBill(billId) {
            if (!confirm('Are you sure you want to delete this bill?')) {
                return;
            }

            try {
                showLoading(true);
                const response = await fetch(`${BILL_API_URL}/${billId}`, {
                    method: 'DELETE',
                });

                if (response.ok) {
                    showSuccess('Bill deleted successfully!');
                    await fetchBills();
                } else {
                    const errorText = await response.text();
                    throw new Error(errorText);
                }
            } catch (error) {
                console.error('Error deleting bill:', error);
                showError('Failed to delete bill: ' + error.message);
            } finally {
                showLoading(false);
            }
        }

        async function viewBillDetails(billId) {
            const bill = allBills.find(b => b.billId === billId);
            if (!bill) {
                showError('Bill not found');
                return;
            }

            const monthName = new Date(bill.billingYear, bill.billingMonth - 1, 1).toLocaleDateString('en-US', { month: 'long', year: 'numeric' });

            const detailsHtml = `
                <div class="card border-0 shadow-sm">
                    <div class="card-header bg-gray-100 border-0 py-3">
                        <h5 class="mb-0 text-gray-800">Bill Details - ${monthName}</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <p class="text-gray-700"><strong>Bill ID:</strong> ${bill.billId}</p>
                                <p class="text-gray-700"><strong>Consumer:</strong> ${bill.consumerName}</p>
                                <p class="text-gray-700"><strong>Meter:</strong> #${bill.meterId}</p>
                                <p class="text-gray-700"><strong>Tariff:</strong> ${bill.tariffName}</p>
                            </div>
                            <div class="col-md-6">
                                <p class="text-gray-700"><strong>Billing Period:</strong> ${monthName}</p>
                                <p class="text-gray-700"><strong>Billing Date:</strong> ${new Date(bill.billingDate).toLocaleDateString('en-IN')}</p>
                                <p class="text-gray-700"><strong>Status:</strong> <span class="badge bg-${bill.billStatus === 'Paid' ? 'success' : bill.billStatus === 'Overdue' ? 'danger' : 'warning'}">${bill.billStatus}</span></p>
                            </div>
                        </div>
                        <hr>
                        <div class="row">
                            <div class="col-md-6">
                                <h6 class="text-gray-800">Consumption Details</h6>
                                <p class="text-gray-700"><strong>Total Consumption:</strong> ${(bill.totalConsumptionKwh || 0).toFixed(2)} kWh</p>
                            </div>
                            <div class="col-md-6">
                                <h6 class="text-gray-800">Billing Amount</h6>
                                <p class="text-gray-700"><strong>Total Amount:</strong> <span class="text-success fs-4">₹${(bill.totalAmount || 0).toFixed(2)}</span></p>
                            </div>
                        </div>
                    </div>
                </div>
            `;

            // Create a simple modal for showing details
            const modalHtml = `
                <div class="modal fade" id="billDetailsModal" tabindex="-1">
                    <div class="modal-dialog modal-lg">
                        <div class="modal-content border-0 shadow">
                            <div class="modal-header bg-gray-100 border-0">
                                <h5 class="modal-title text-gray-800">Bill Details</h5>
                                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                            </div>
                            <div class="modal-body">
                                ${detailsHtml}
                            </div>
                            <div class="modal-footer bg-gray-100 border-0">
                                <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Close</button>
                            </div>
                        </div>
                    </div>
                </div>
            `;

            // Remove existing modal if any
            const existingModal = document.getElementById('billDetailsModal');
            if (existingModal) {
                existingModal.remove();
            }

            // Add new modal
            document.body.insertAdjacentHTML('beforeend', modalHtml);
            const modal = new bootstrap.Modal(document.getElementById('billDetailsModal'));
            modal.show();
        }

        function showLoading(show) {
            const indicator = document.getElementById('loadingIndicator');
            if (indicator) {
                indicator.style.display = show ? 'block' : 'none';
            }
        }
    </script>
}