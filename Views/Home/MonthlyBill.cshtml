@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Monthly Bills";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-file-earmark-text"></i> Monthly Bills Management</h2>
        <button class="btn btn-success" data-bs-toggle="modal" data-bs-target="#billModal" onclick="prepareAddBill()">
            <i class="bi bi-plus-circle"></i> Add Monthly Bill
        </button>
    </div>

    <div id="loadingIndicator" class="text-center my-4" style="display: none;">
        <div class="spinner-border text-success" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
        <p class="mt-2">Loading bills...</p>
    </div>
    <div id="errorMessage" class="alert alert-danger alert-dismissible fade show" style="display: none;">
        <button type="button" class="btn-close" onclick="showError('')"></button>
        <span id="errorText"></span>
    </div>
    <div id="successMessage" class="alert alert-success alert-dismissible fade show" style="display: none;">
        <button type="button" class="btn-close" onclick="showSuccess('')"></button>
        <span id="successText"></span>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th>Bill ID</th>
                            <th>Consumer</th>
                            <th>Meter</th>
                            <th>Billing Date</th>
                            <th>Consumption (kWh)</th>
                            <th>Amount (₹)</th>
                            <th>Status</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody id="billsTableBody">
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- Add/Edit Modal -->
<div class="modal fade" id="billModal" tabindex="-1" aria-labelledby="billModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="billModalLabel">Add Monthly Bill</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form id="billForm">
                    <input type="hidden" id="isEdit" value="false" />
                    <input type="hidden" id="billId" value="0" />

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="consumerId" class="form-label">Consumer <span class="text-danger">*</span></label>
                            <select class="form-select" id="consumerId" required>
                                <option value="">-- Select Consumer --</option>
                            </select>
                            <small class="form-text text-muted">Select the consumer for this bill</small>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="meterId" class="form-label">Meter <span class="text-danger">*</span></label>
                            <select class="form-select" id="meterId" required>
                                <option value="">-- Select Meter --</option>
                            </select>
                            <small class="form-text text-muted">Select the meter associated with this bill</small>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="billingDate" class="form-label">Billing Date <span class="text-danger">*</span></label>
                            <input type="date" class="form-control" id="billingDate" required>
                            <small class="form-text text-muted">Date when the bill was generated</small>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="billStatus" class="form-label">Bill Status <span class="text-danger">*</span></label>
                            <select class="form-select" id="billStatus" required>
                                <option value="">-- Select Status --</option>
                                <option value="Pending">Pending</option>
                                <option value="Paid">Paid</option>
                                <option value="Overdue">Overdue</option>
                                <option value="Cancelled">Cancelled</option>
                            </select>
                            <small class="form-text text-muted">Current status of the bill</small>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="totalConsumptionKwh" class="form-label">Total Consumption (kWh) <span class="text-danger">*</span></label>
                            <input type="number" step="0.01" class="form-control" id="totalConsumptionKwh" required onchange="calculateAmount()">
                            <small class="form-text text-muted">Total electricity consumed in kWh</small>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="totalAmount" class="form-label">Total Amount (₹) <span class="text-danger">*</span></label>
                            <input type="number" step="0.01" class="form-control" id="totalAmount" required>
                            <small class="form-text text-muted">Total bill amount</small>
                        </div>
                    </div>

                    <div class="alert alert-info" role="alert">
                        <i class="bi bi-info-circle"></i> <strong>Note:</strong> You can manually adjust the total amount or it will be calculated based on consumption and rate.
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-success" onclick="saveBill()">
                    <i class="bi bi-save"></i> Save Bill
                </button>
            </div>
        </div>
    </div>
</div>

<!-- View Details Modal -->
<div class="modal fade" id="viewBillModal" tabindex="-1" aria-labelledby="viewBillModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="viewBillModalLabel">Bill Details</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="row mb-3">
                    <div class="col-12">
                        <div class="card border-success">
                            <div class="card-header bg-success text-white">
                                <h6 class="mb-0"><i class="bi bi-info-circle"></i> Bill Information</h6>
                            </div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="row mb-2">
                                            <div class="col-5 fw-bold">Bill ID:</div>
                                            <div class="col-7" id="viewBillId"></div>
                                        </div>
                                        <div class="row mb-2">
                                            <div class="col-5 fw-bold">Consumer:</div>
                                            <div class="col-7" id="viewConsumerName"></div>
                                        </div>
                                        <div class="row mb-2">
                                            <div class="col-5 fw-bold">Meter Number:</div>
                                            <div class="col-7" id="viewmeterId"></div>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="row mb-2">
                                            <div class="col-5 fw-bold">Billing Date:</div>
                                            <div class="col-7" id="viewBillingDate"></div>
                                        </div>
                                        <div class="row mb-2">
                                            <div class="col-5 fw-bold">Status:</div>
                                            <div class="col-7" id="viewStatus"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-12">
                        <div class="card border-success">
                            <div class="card-header bg-success text-white">
                                <h6 class="mb-0"><i class="bi bi-lightning-charge"></i> Consumption & Billing</h6>
                            </div>
                            <div class="card-body">
                                <div class="row text-center">
                                    <div class="col-md-6">
                                        <div class="p-3 bg-light rounded">
                                            <h6 class="text-muted">Total Consumption</h6>
                                            <h3 id="viewTotalConsumption" class="text-success"></h3>
                                            <small class="text-muted">kWh</small>
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <div class="p-3 bg-warning bg-opacity-25 rounded">
                                            <h6 class="text-muted">Total Amount</h6>
                                            <h3 id="viewTotalAmount" class="text-success"></h3>
                                            <small class="text-muted">₹</small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
    <!-- Bill Generation Section -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-success text-white">
            <h5 class="mb-0"><i class="bi bi-lightning-charge"></i> Generate Monthly Bills</h5>
        </div>
        <div class="card-body">
            <div class="row g-3 align-items-end">
                <div class="col-md-3">
                    <label for="genMonth" class="form-label">Month</label>
                    <select class="form-select" id="genMonth">
                        <option value="1">January</option>
                        <option value="2">February</option>
                        <option value="3">March</option>
                        <option value="4">April</option>
                        <option value="5">May</option>
                        <option value="6">June</option>
                        <option value="7">July</option>
                        <option value="8">August</option>
                        <option value="9">September</option>
                        <option value="10">October</option>
                        <option value="11">November</option>
                        <option value="12">December</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label for="genYear" class="form-label">Year</label>
                    <input type="number" class="form-control" id="genYear" min="2000" max="2100" value="@DateTime.Now.Year">
                </div>
                <div class="col-md-4">
                    <label for="meterFilter" class="form-label">Filter by Meter (Optional)</label>
                    <select class="form-select" id="meterFilter">
                        <option value="0">All Meters</option>
                    </select>
                </div>
                <div class="col-md-2">
                    <button class="btn btn-success w-100" onclick="generateBills()">
                        <i class="bi bi-gear"></i> Generate Bills
                    </button>
                </div>
            </div>

            <!-- Preview Section -->
            <div id="previewSection" class="mt-4" style="display: none;">
                <h6>Preview Daily Readings</h6>
                <div class="table-responsive">
                    <table class="table table-sm table-bordered">
                        <thead class="table-light">
                            <tr>
                                <th>Date</th>
                                <th>Time of Day</th>
                                <th>Consumption (kWh)</th>
                                <th>Amount (₹)</th>
                            </tr>
                        </thead>
                        <tbody id="previewTableBody">
                        </tbody>
                        <tfoot class="table-warning">
                            <tr>
                                <td colspan="2" class="fw-bold">Monthly Total</td>
                                <td id="previewTotalConsumption" class="fw-bold">0.00</td>
                                <td id="previewTotalAmount" class="fw-bold">0.00</td>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- Filter Section -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-info text-white">
            <h5 class="mb-0"><i class="bi bi-funnel"></i> Filter Bills</h5>
        </div>
        <div class="card-body">
            <div class="row g-3 align-items-end">
                <div class="col-md-3">
                    <label for="filterMonth" class="form-label">Month</label>
                    <select class="form-select" id="filterMonth" onchange="filterBills()">
                        <option value="0">All Months</option>
                        <option value="1">January</option>
                        <option value="2">February</option>
                        <option value="3">March</option>
                        <option value="4">April</option>
                        <option value="5">May</option>
                        <option value="6">June</option>
                        <option value="7">July</option>
                        <option value="8">August</option>
                        <option value="9">September</option>
                        <option value="10">October</option>
                        <option value="11">November</option>
                        <option value="12">December</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label for="filterYear" class="form-label">Year</label>
                    <input type="number" class="form-control" id="filterYear" min="2000" max="2100"
                           value="@DateTime.Now.Year" onchange="filterBills()">
                </div>
                <div class="col-md-3">
                    <label for="filterMeter" class="form-label">Meter</label>
                    <select class="form-select" id="filterMeter" onchange="filterBills()">
                        <option value="0">All Meters</option>
                    </select>
                </div>
                <div class="col-md-3">
                    <label for="filterStatus" class="form-label">Status</label>
                    <select class="form-select" id="filterStatus" onchange="filterBills()">
                        <option value="">All Status</option>
                        <option value="Pending">Pending</option>
                        <option value="Paid">Paid</option>
                        <option value="Overdue">Overdue</option>
                        <option value="Cancelled">Cancelled</option>
                    </select>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const API_BASE_URL = '@Configuration["ApiBaseUrl"]';
        const BILL_API_URL = `${API_BASE_URL}/api/MonthlyBill`;
        const CONSUMER_API_URL = `${API_BASE_URL}/api/Consumer`;
        const METER_API_URL = `${API_BASE_URL}/api/Meter`;

        let allBills = [];
        let allConsumers = [];
        let allMeters = [];
        let billModal;
        let viewBillModal;

        document.addEventListener('DOMContentLoaded', function() {
            billModal = new bootstrap.Modal(document.getElementById('billModal'));
            viewBillModal = new bootstrap.Modal(document.getElementById('viewBillModal'));
            fetchBills();
            fetchConsumers();
            fetchMeters();
        });
                // Enhanced JavaScript functions
        async function generateBills() {
            const month = parseInt(document.getElementById('genMonth').value);
            const year = parseInt(document.getElementById('genYear').value);
            const meterId = parseInt(document.getElementById('meterFilter').value) || 0;

            if (!month || !year) {
                showError('Please select both month and year');
                return;
            }

            // First, check if bills already exist for this month
            try {
                const checkResponse = await fetch(`${BILL_API_URL}/CheckBillsExist/${month}/${year}`);
                if (checkResponse.ok) {
                    const billsExist = await checkResponse.json();
                    if (billsExist && !confirm(`Bills for ${month}/${year} already exist. Regenerate?`)) {
                        return;
                    }
                }
            } catch (error) {
                console.error('Error checking existing bills:', error);
            }

            try {
                showLoading(true);
                const response = await fetch(`${BILL_API_URL}/GenerateBills`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ month, year }),
                });

                if (response.ok) {
                    const result = await response.json();
                    showSuccess(result.message);
                    await fetchBills(); // Refresh the bills list
                } else {
                    const errorText = await response.text();
                    showError(`Failed to generate bills: ${errorText}`);
                }
            } catch (error) {
                console.error('Error generating bills:', error);
                showError('An error occurred while generating bills.');
            } finally {
                showLoading(false);
            }
        }

        async function previewDailyReadings() {
            const month = parseInt(document.getElementById('genMonth').value);
            const year = parseInt(document.getElementById('genYear').value);
            const meterId = parseInt(document.getElementById('meterFilter').value);

            if (!month || !year || !meterId) {
                return;
            }

            try {
                const response = await fetch(`${BILL_API_URL}/DailyReadings/${meterId}/${month}/${year}`);
                if (response.ok) {
                    const readings = await response.json();
                    displayPreview(readings);
                }
            } catch (error) {
                console.error('Error loading daily readings:', error);
            }
        }

        function displayPreview(readings) {
            const tbody = document.getElementById('previewTableBody');
            const previewSection = document.getElementById('previewSection');

            if (!readings || readings.length === 0) {
                tbody.innerHTML = '<tr><td colspan="4" class="text-center">No daily readings found for selected period</td></tr>';
                previewSection.style.display = 'block';
                return;
            }

            let totalConsumption = 0;
            let totalAmount = 0;

            tbody.innerHTML = '';
            readings.forEach(reading => {
                totalConsumption += reading.consumptionKwh;
                totalAmount += reading.amount;

                const rowHtml = `
                    <tr>
                        <td>${new Date(reading.readingDate).toLocaleDateString('en-IN')}</td>
                        <td>${reading.todRuleName}</td>
                        <td>${reading.consumptionKwh.toFixed(2)}</td>
                        <td>₹${reading.amount.toFixed(2)}</td>
                    </tr>
                `;
                tbody.insertAdjacentHTML('beforeend', rowHtml);
            });

            document.getElementById('previewTotalConsumption').textContent = totalConsumption.toFixed(2);
            document.getElementById('previewTotalAmount').textContent = '₹' + totalAmount.toFixed(2);
            previewSection.style.display = 'block';
        }

        function filterBills() {
            const month = parseInt(document.getElementById('filterMonth').value) || 0;
            const year = parseInt(document.getElementById('filterYear').value) || 0;
            const meterId = parseInt(document.getElementById('filterMeter').value) || 0;
            const status = document.getElementById('filterStatus').value;

            let filteredBills = allBills;

            if (month > 0) {
                filteredBills = filteredBills.filter(bill => bill.billingMonth === month);
            }

            if (year > 0) {
                filteredBills = filteredBills.filter(bill => bill.billingYear === year);
            }

            if (meterId > 0) {
                filteredBills = filteredBills.filter(bill => bill.meterId === meterId);
            }

            if (status) {
                filteredBills = filteredBills.filter(bill => bill.billStatus === status);
            }

            renderBillsTable(filteredBills);
        }

        // Update the existing renderBillsTable function to include month/year
        function renderBillsTable(bills) {
            const tbody = document.getElementById('billsTableBody');
            tbody.innerHTML = '';

            if (bills && bills.length > 0) {
                bills.forEach(bill => {
                    const billingDate = new Date(bill.billingDate).toLocaleDateString('en-IN');
                    const period = `${bill.billingMonth}/${bill.billingYear}`;

                    let statusBadge;
                    switch(bill.billStatus) {
                        case 'Paid':
                            statusBadge = '<span class="badge bg-success">Paid</span>';
                            break;
                        case 'Overdue':
                            statusBadge = '<span class="badge bg-danger">Overdue</span>';
                            break;
                        case 'Cancelled':
                            statusBadge = '<span class="badge bg-secondary">Cancelled</span>';
                            break;
                        default:
                            statusBadge = '<span class="badge bg-warning text-dark">Pending</span>';
                    }

                    const rowHtml = `
                        <tr>
                            <td><strong>${bill.billId}</strong></td>
                            <td>${bill.consumerName}</td>
                            <td>${bill.meterId}</td>
                            <td>${billingDate}</td>
                            <td>${period}</td>
                            <td>${bill.totalConsumptionKwh.toFixed(2)}</td>
                            <td>₹${bill.totalAmount.toFixed(2)}</td>
                            <td>${statusBadge}</td>
                            <td>
                                <button class="btn btn-sm btn-outline-success me-1" onclick="viewBillDetails(${bill.billId})" title="View Details">
                                    <i class="bi bi-eye"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-success me-1" onclick="editBill(${bill.billId})" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-danger" onclick="deleteBill(${bill.billId})" title="Delete">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    `;
                    tbody.insertAdjacentHTML('beforeend', rowHtml);
                });
            } else {
                tbody.innerHTML = '<tr><td colspan="9" class="text-center">No bills found matching the criteria.</td></tr>';
            }
        }

        // Update table header to include Period column
        // Add this after the existing table header setup
        function updateTableHeader() {
            const thead = document.querySelector('thead tr');
            if (thead && !thead.innerHTML.includes('Period')) {
                thead.innerHTML = `
                    <th>Bill ID</th>
                    <th>Consumer</th>
                    <th>Meter</th>
                    <th>Billing Date</th>
                    <th>Period</th>
                    <th>Consumption (kWh)</th>
                    <th>Amount (₹)</th>
                    <th>Status</th>
                    <th>Actions</th>
                `;
            }
        }

        // Initialize filters when page loads
        document.addEventListener('DOMContentLoaded', function() {
            // ... existing code ...
            updateTableHeader();

            // Set current month/year in generation section
            const currentDate = new Date();
            document.getElementById('genMonth').value = currentDate.getMonth() + 1;
            document.getElementById('filterMonth').value = currentDate.getMonth() + 1;

            // Populate meter filters
            populateMeterFilters();
        });

        function populateMeterFilters() {
            const meterFilter = document.getElementById('meterFilter');
            const filterMeter = document.getElementById('filterMeter');

            if (allMeters.length > 0) {
                allMeters.forEach(meter => {
                    meterFilter.innerHTML += `<option value="${meter.meterId}">Meter ${meter.meterId}</option>`;
                    filterMeter.innerHTML += `<option value="${meter.meterId}">Meter ${meter.meterId}</option>`;
                });
            }
        }

        async function fetchBills() {
            try {
                showLoading(true);
                showError('');
                const response = await fetch(`${BILL_API_URL}/AllMonthlyBills`);

                if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);

                allBills = await response.json();
                renderBillsTable(allBills);
                showLoading(false);
            } catch (error) {
                console.error('Error loading bills:', error);
                showError('Failed to load bills: ' + error.message);
                showLoading(false);
            }
        }

        async function fetchConsumers() {
            try {
                const response = await fetch(`${CONSUMER_API_URL}/AllConsumers`);
                if (response.ok) {
                    allConsumers = await response.json();
                    populateConsumersDropdown();
                }
            } catch (error) {
                console.error('Error loading consumers:', error);
            }
        }

        async function fetchMeters() {
            try {
                const response = await fetch(`${METER_API_URL}/AllMeters`);
                if (response.ok) {
                    allMeters = await response.json();
                    populateMetersDropdown();
                }
            } catch (error) {
                console.error('Error loading meters:', error);
            }
        }

        function populateConsumersDropdown() {
            const select = document.getElementById('consumerId');
            select.innerHTML = '<option value="">-- Select Consumer --</option>';
            allConsumers.forEach(consumer => {
                select.innerHTML += `<option value="${consumer.consumerId}">${consumer.name}</option>`;
            });
        }

        function populateMetersDropdown() {
            const select = document.getElementById('meterId');
            select.innerHTML = '<option value="">-- Select Meter --</option>';
            allMeters.forEach(meter => {
                select.innerHTML += `<option value="${meter.meterId}">${meter.meterId}</option>`;
            });
        }

        function renderBillsTable(bills) {
            const tbody = document.getElementById('billsTableBody');
            tbody.innerHTML = '';

            if (bills && bills.length > 0) {
                bills.forEach(bill => {
                    const billingDate = new Date(bill.billingDate).toLocaleDateString('en-IN');

                    let statusBadge;
                    switch(bill.billStatus) {
                        case 'Paid':
                            statusBadge = '<span class="badge bg-success">Paid</span>';
                            break;
                        case 'Overdue':
                            statusBadge = '<span class="badge bg-danger">Overdue</span>';
                            break;
                        case 'Cancelled':
                            statusBadge = '<span class="badge bg-secondary">Cancelled</span>';
                            break;
                        default:
                            statusBadge = '<span class="badge bg-warning text-dark">Pending</span>';
                    }

                    const rowHtml = `
                        <tr>
                            <td><strong>${bill.billId}</strong></td>
                            <td>${bill.consumerName}</td>
                            <td>${bill.meterId}</td>
                            <td>${billingDate}</td>
                            <td>${bill.totalConsumptionKwh.toFixed(2)}</td>
                            <td>₹${bill.totalAmount.toFixed(2)}</td>
                            <td>${statusBadge}</td>
                            <td>
                                <button class="btn btn-sm btn-outline-success me-1" onclick="viewBillDetails(${bill.billId})" title="View Details">
                                    <i class="bi bi-eye"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-success me-1" onclick="editBill(${bill.billId})" title="Edit">
                                    <i class="bi bi-pencil"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-danger" onclick="deleteBill(${bill.billId})" title="Delete">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    `;
                    tbody.insertAdjacentHTML('beforeend', rowHtml);
                });
            } else {
                tbody.innerHTML = '<tr><td colspan="8" class="text-center">No bills found. Click "Add Monthly Bill" to create one.</td></tr>';
            }
        }

        function prepareAddBill() {
            document.getElementById('billModalLabel').textContent = 'Add Monthly Bill';
            document.getElementById('billForm').reset();
            document.getElementById('isEdit').value = 'false';
            document.getElementById('billId').value = '0';

            // Set default billing date to today
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('billingDate').value = today;
        }

        async function editBill(billId) {
            const bill = allBills.find(b => b.billId === billId);
            if (!bill) {
                showError('Bill not found');
                return;
            }

            document.getElementById('billModalLabel').textContent = 'Edit Monthly Bill';
            document.getElementById('isEdit').value = 'true';
            document.getElementById('billId').value = bill.billId;
            document.getElementById('consumerId').value = bill.consumerId;
            document.getElementById('meterId').value = bill.meterId;
            document.getElementById('billingDate').value = bill.billingDate;
            document.getElementById('totalConsumptionKwh').value = bill.totalConsumptionKwh;
            document.getElementById('totalAmount').value = bill.totalAmount;
            document.getElementById('billStatus').value = bill.billStatus;

            billModal.show();
        }

        async function saveBill() {
            const isEdit = document.getElementById('isEdit').value === 'true';
            const billId = parseInt(document.getElementById('billId').value);
            const consumerId = parseInt(document.getElementById('consumerId').value);
            const meterId = parseInt(document.getElementById('meterId').value);
            const billingDate = document.getElementById('billingDate').value;
            const totalConsumptionKwh = parseFloat(document.getElementById('totalConsumptionKwh').value);
            const totalAmount = parseFloat(document.getElementById('totalAmount').value);
            const billStatus = document.getElementById('billStatus').value;

            if (!consumerId || !meterId || !billingDate || !totalConsumptionKwh || !totalAmount || !billStatus) {
                showError('Please fill all required fields');
                return;
            }

            if (totalConsumptionKwh < 0 || totalAmount < 0) {
                showError('Consumption and amount must be positive values');
                return;
            }

            const billData = {
                billId: isEdit ? billId : null,
                consumerId: consumerId,
                meterId: meterId,
                billingDate: billingDate,
                totalConsumptionKwh: totalConsumptionKwh,
                totalAmount: totalAmount,
                billStatus: billStatus
            };

            const endpoint = isEdit ? `${BILL_API_URL}/Update` : `${BILL_API_URL}/Create`;
            const method = isEdit ? 'PUT' : 'POST';

            try {
                const response = await fetch(endpoint, {
                    method: method,
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(billData),
                });

                if (response.ok) {
                    billModal.hide();
                    showSuccess(isEdit ? 'Bill updated successfully!' : 'Bill created successfully!');
                    await fetchBills();
                } else {
                    const errorText = await response.text();
                    showError(`Failed to save bill: ${errorText}`);
                }
            } catch (error) {
                console.error('Error saving bill:', error);
                showError('An error occurred while saving the bill.');
            }
        }

        async function deleteBill(billId) {
            if (!confirm('Are you sure you want to delete this bill?')) {
                return;
            }

            try {
                const response = await fetch(`${BILL_API_URL}/${billId}`, {
                    method: 'DELETE',
                });

                if (response.ok) {
                    showSuccess('Bill deleted successfully!');
                    await fetchBills();
                } else {
                    const errorText = await response.text();
                    showError(`Failed to delete bill: ${errorText}`);
                }
            } catch (error) {
                console.error('Error deleting bill:', error);
                showError('An error occurred while deleting the bill.');
            }
        }

        function viewBillDetails(billId) {
            const bill = allBills.find(b => b.billId === billId);
            if (!bill) {
                showError('Bill not found');
                return;
            }

            const billingDate = new Date(bill.billingDate).toLocaleDateString('en-IN', {
                year: 'numeric',
                month: 'long',
                day: 'numeric'
            });

            let statusBadge;
            switch(bill.billStatus) {
                case 'Paid':
                    statusBadge = '<span class="badge bg-success"><i class="bi bi-check-circle"></i> PAID</span>';
                    break;
                case 'Overdue':
                    statusBadge = '<span class="badge bg-danger"><i class="bi bi-exclamation-triangle"></i> OVERDUE</span>';
                    break;
                case 'Cancelled':
                    statusBadge = '<span class="badge bg-secondary"><i class="bi bi-x-circle"></i> CANCELLED</span>';
                    break;
                default:
                    statusBadge = '<span class="badge bg-warning text-dark"><i class="bi bi-clock"></i> PENDING</span>';
            }

            document.getElementById('viewBillId').textContent = bill.billId;
            document.getElementById('viewConsumerName').textContent = bill.consumerName;
            document.getElementById('viewmeterId').textContent = bill.meterId;
            document.getElementById('viewBillingDate').textContent = billingDate;
            document.getElementById('viewStatus').innerHTML = statusBadge;
            document.getElementById('viewTotalConsumption').textContent = bill.totalConsumptionKwh.toFixed(2);
            document.getElementById('viewTotalAmount').textContent = '₹ ' + bill.totalAmount.toFixed(2);

            viewBillModal.show();
        }

        function calculateAmount() {
            // Optional: You can add logic here to calculate amount based on consumption
            // For example, if you have a rate per kWh
            const consumption = parseFloat(document.getElementById('totalConsumptionKwh').value) || 0;
            const defaultRate = 7.5; // Default rate per kWh in rupees

            if (consumption > 0 && !document.getElementById('totalAmount').value) {
                const calculatedAmount = consumption * defaultRate;
                document.getElementById('totalAmount').value = calculatedAmount.toFixed(2);
            }
        }

        function showLoading(show) {
            document.getElementById('loadingIndicator').style.display = show ? 'block' : 'none';
        }

        function showError(message) {
            const errorDiv = document.getElementById('errorMessage');
            const errorText = document.getElementById('errorText');
            if (message) {
                errorText.textContent = message;
                errorDiv.style.display = 'block';
                setTimeout(() => { errorDiv.style.display = 'none'; }, 5000);
            } else {
                errorDiv.style.display = 'none';
            }
        }

        function showSuccess(message) {
            const successDiv = document.getElementById('successMessage');
            const successText = document.getElementById('successText');
            if (message) {
                successText.textContent = message;
                successDiv.style.display = 'block';
                setTimeout(() => { successDiv.style.display = 'none'; }, 3000);
            } else {
                successDiv.style.display = 'none';
            }
        }
    </script>
}