@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Daily Meter Reading";
}
<style>
    :root {
        --card-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        --card-hover-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    }

    body {
        background-color: #f8f9fa;
        padding: 0;
    }

    .card {
        border-radius: 12px;
        border: none;
        transition: all 0.3s ease;
        box-shadow: var(--card-shadow);
    }

        .card:hover {
            box-shadow: var(--card-hover-shadow);
        }

    .card-header {
        border-radius: 12px 12px 0 0 !important;
        border-bottom: 1px solid rgba(0,0,0,0.05);
    }

    .form-control, .form-select {
        border-radius: 8px;
        border: 1px solid #e9ecef;
        transition: all 0.2s ease;
    }

        .form-control:focus, .form-select:focus {
            border-color: #198754;
            box-shadow: 0 0 0 0.2rem rgba(25, 135, 84, 0.25);
        }

    .btn {
        border-radius: 8px;
        transition: all 0.2s ease;
    }

        .btn:hover {
            transform: translateY(-1px);
        }

    .table {
        border-radius: 8px;
        overflow: hidden;
    }

        .table th {
            border-top: none;
            font-weight: 600;
            background-color: #f8f9fa;
        }

        .table input[type="time"],
        .table input[type="number"],
        .table select {
            font-size: 0.875rem;
            border-radius: 6px;
        }

        .table td {
            vertical-align: middle;
        }

    .validation-error {
        border-color: #dc3545 !important;
    }

    .stats-card {
        border-top: 4px solid !important;
    }

        .stats-card.total-consumption {
            border-color: #198754 !important;
        }

        .stats-card.base-amount {
            border-color: #0dcaf0 !important;
        }

        .stats-card.adjustment {
            border-color: #ffc107 !important;
        }

        .stats-card.total-amount {
            border-color: #198754 !important;
        }

    .badge {
        font-size: 0.75em;
        padding: 0.35em 0.65em;
    }

    .alert {
        border-radius: 8px;
        border: none;
    }

    .modal-header {
        border-radius: 12px 12px 0 0;
    }

    .modal-footer {
        border-radius: 0 0 12px 12px;
    }

    .input-group-text {
        border-radius: 8px;
        border: 1px solid #e9ecef;
        background-color: #f8f9fa;
    }

    .bg-opacity-10 {
        background-opacity: 0.1 !important;
    }

    .consumption-display {
        font-size: 0.875rem;
        font-weight: 600;
        color: #198754;
    }
</style>

<div class="container-fluid">
    <!-- Header Section -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2 class="fw-bold text-dark mb-1">
                <i class="bi bi-calendar-day text-success me-2"></i>Daily Meter Reading Entry
            </h2>
            <p class="text-muted mb-0">Record electricity consumption readings with Time-of-Day pricing</p>
        </div>
        <div>
            <button onclick="viewAllReadings()" class="btn btn-outline-info me-2">
                <i class="bi bi-list-ul me-2"></i> View All Readings
            </button>
            <button onclick="viewMonthlyBills()" class="btn btn-success">
                <i class="bi bi-file-earmark-text me-2"></i> Monthly Bills
            </button>
        </div>
    </div>

    <!-- Messages -->
    <div id="errorMessage" class="alert alert-danger border-0 shadow-sm mb-4" style="display: none;">
        <div class="d-flex align-items-center">
            <i class="bi bi-exclamation-triangle-fill me-3 fs-5"></i>
            <div>
                <h6 class="alert-heading mb-1">Action Required</h6>
                <span id="errorText" class="small"></span>
            </div>
        </div>
    </div>

    <div id="successMessage" class="alert alert-success border-0 shadow-sm mb-4" style="display: none;">
        <div class="d-flex align-items-center">
            <i class="bi bi-check-circle-fill me-3 fs-5"></i>
            <div>
                <h6 class="alert-heading mb-1">Success</h6>
                <span id="successText" class="small"></span>
            </div>
        </div>
    </div>

    <!-- Reading Information Card -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-success text-white">
            <h5 class="mb-0"><i class="bi bi-info-circle me-2"></i>Reading Information</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-6 mb-3">
                    <label for="meterId" class="form-label fw-semibold">Select Meter <span class="text-danger">*</span></label>
                    <select class="form-select" id="meterId" onchange="loadMeterInfo()">
                        <option value="">-- Select Meter --</option>
                    </select>
                </div>
                <div class="col-md-6 mb-3">
                    <label for="readingDate" class="form-label fw-semibold">Reading Date <span class="text-danger">*</span></label>
                    <input type="date" class="form-control" id="readingDate" value="">
                </div>
            </div>
            <div id="meterInfoDisplay" class="alert alert-info border-0" style="display: none;">
                <div class="d-flex align-items-center">
                    <i class="bi bi-lightbulb me-3 fs-4"></i>
                    <div>
                        <h6 class="alert-heading mb-1">Meter Information</h6>
                        <span id="meterDetails" class="small"></span>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- TOD Readings Card -->
    <div class="card shadow-sm mb-4">
        <div class="card-header bg-success text-white d-flex justify-content-between align-items-center">
            <h5 class="mb-0"><i class="bi bi-clock-history me-2"></i>TOD Readings</h5>
            <span class="badge bg-light text-dark" id="todRulesCount">0 rules loaded</span>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th style="width: 25%; color: black;">TOD Rule</th>
                            <th style="width: 12%; color: black;">Start Time</th>
                            <th style="width: 12%; color: black;">End Time</th>
                            <th style="width: 17%; color: black;">Previous Reading</th>
                            <th style="width: 17%; color: black;">Current Reading</th>
                            <th style="width: 17%; color: black;">Consumption</th>
                        </tr>
                    </thead>
                    <tbody id="readingsTableBody">
                        <tr>
                            <td colspan="6" class="text-center text-muted py-4">
                                <i class="bi bi-inbox display-6 d-block mb-2"></i>
                                Select a meter to load TOD slots
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!-- Action Buttons -->
    <div class="card shadow-sm">
        <div class="card-body">
            <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                <button class="btn btn-outline-secondary" onclick="clearForm()">
                    <i class="bi bi-x-circle me-2"></i> Clear
                </button>
                <button class="btn btn-success px-4" onclick="saveReadings()">
                    <i class="bi bi-save me-2"></i> Save Readings
                </button>
            </div>
        </div>
    </div>
</div>

<!-- View Readings Modal -->
<div class="modal fade" id="viewReadingsModal" tabindex="-1">
    <div class="modal-dialog modal-xl">
        <div class="modal-content border-0 shadow">
            <div class="modal-header bg-success text-white">
                <h5 class="modal-title"><i class="bi bi-list-ul me-2"></i>Daily Meter Readings</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <div class="row mb-3">
                    <div class="col-md-4">
                        <label class="form-label fw-semibold">Filter by Meter:</label>
                        <select class="form-select" id="filterMeterId" onchange="loadAllReadings()">
                            <option value="">All Meters</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">Start Date:</label>
                        <input type="date" class="form-control" id="filterStartDate" onchange="loadAllReadings()">
                    </div>
                    <div class="col-md-3">
                        <label class="form-label fw-semibold">End Date:</label>
                        <input type="date" class="form-control" id="filterEndDate" onchange="loadAllReadings()">
                    </div>
                    <div class="col-md-2 d-flex align-items-end">
                        <button class="btn btn-outline-secondary w-100" onclick="clearFilters()">
                            <i class="bi bi-x-circle me-2"></i> Clear
                        </button>
                    </div>
                </div>
                <div class="table-responsive" style="max-height: 500px; overflow-y: auto;">
                    <table class="table table-bordered table-hover table-sm">
                        <thead class="table-dark sticky-top">
                            <tr>
                                <th style="color: black;">Date</th>
                                <th style="color: black;">Meter</th>
                                <th style="color: black;">TOD Rule</th>
                                <th style="color: black;">Time Slot</th>
                                <th style="color: black;">Previous</th>
                                <th style="color: black;">Current</th>
                                <th style="color: black;">Consumption</th>
                                <th style="color: black;">Amount</th>
                                <th style="color: black;">Actions</th>
                            </tr>
                        </thead>
                        <tbody id="allReadingsTableBody">
                            <tr>
                                <td colspan="9" class="text-center text-muted py-4">
                                    <i class="bi bi-inbox display-6 d-block mb-2"></i>
                                    Select filters to view readings
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    const DAILY_READING_API_URL = `${API_BASE_URL}/api/DailyMeterReading`;
    const TOD_RULE_API_URL = `${API_BASE_URL}/api/TodRule`;
    const METER_API_URL = `${API_BASE_URL}/api/Meter`;

    let allTodRules = [];
    let allMeters = [];
    let currentMeterLatestReading = 0;
    let currentMeterApplicableRules = [];

    function showError(message) {
        document.getElementById('errorText').textContent = message;
        document.getElementById('errorMessage').style.display = 'block';
    }

    function hideError() {
        document.getElementById('errorMessage').style.display = 'none';
    }

    function showSuccess(message) {
        document.getElementById('successText').textContent = message;
        document.getElementById('successMessage').style.display = 'block';
        setTimeout(hideSuccess, 5000);
    }

    function hideSuccess() {
        document.getElementById('successMessage').style.display = 'none';
    }

    document.addEventListener('DOMContentLoaded', function () {
        document.getElementById('readingDate').valueAsDate = new Date();

        document.getElementById('readingDate').addEventListener('change', function () {
            if (document.getElementById('meterId').value) {
                checkExistingReadingsForDate();
            }
        });

        fetchAllData();
    });

    async function fetchAllData() {
        try {
            await fetchMeters();
            await fetchTodRules();
        } catch (error) {
            showError('Failed to load initial data. Please refresh the page.');
        }
    }

    async function fetchMeters() {
        try {
            const response = await fetch(`${METER_API_URL}/AllMeters`);
            if (!response.ok) throw new Error('Failed to load meters');
            allMeters = await response.json();
            populateMeterDropdown();
        } catch (error) {
            throw error;
        }
    }

    function populateMeterDropdown() {
        const select = document.getElementById('meterId');
        const filterSelect = document.getElementById('filterMeterId');

        select.innerHTML = '<option value="">-- Select Meter --</option>';
        filterSelect.innerHTML = '<option value="">All Meters</option>';

        allMeters.forEach((meter) => {
            const baseRate = meter.baseRate || meter.tariff?.baseRate || 0;
            const consumerName = meter.consumerName || meter.consumer?.name || 'Unknown';
            const latestReading = meter.latestReading || 0;
            const meterId = meter.meterId;
            const tariffId = meter.tariffId || meter.tariff?.tariffId;

            const option = `<option value="${meterId}"
                        data-meter-number="${meterId}"
                        data-base-rate="${baseRate}"
                        data-latest-reading="${latestReading}"
                        data-consumer-name="${consumerName}"
                        data-tariff-id="${tariffId}">
                        Meter #${meterId} - ${consumerName}
                    </option>`;

            select.innerHTML += option;
            filterSelect.innerHTML += `<option value="${meterId}">Meter #${meterId} - ${consumerName}</option>`;
        });
    }

    async function fetchTodRules() {
        try {
            const response = await fetch(`${TOD_RULE_API_URL}/AllRules`);
            if (!response.ok) throw new Error('Failed to load TOD rules');
            const rules = await response.json();
            allTodRules = rules.filter(r => r.isActive);
            console.log('Total TOD rules loaded:', allTodRules.length);
            console.log('TOD rules by tariff:',
                allTodRules.reduce((acc, rule) => {
                    acc[rule.tariffId] = (acc[rule.tariffId] || 0) + 1;
                    return acc;
                }, {})
            );
        } catch (error) {
            throw error;
        }
    }

        function loadMeterInfo() {
        const select = document.getElementById('meterId');
        const selectedOption = select.options[select.selectedIndex];

        if (select.value) {
            const meterId = selectedOption.dataset.meterNumber;
            const baseRate = parseFloat(selectedOption.dataset.baseRate) || 0;
            const consumerName = selectedOption.dataset.consumerName || 'Unknown';
            const tariffId = parseInt(selectedOption.dataset.tariffId);
            currentMeterLatestReading = parseFloat(selectedOption.dataset.latestReading) || 0;

            console.log('=== METER SELECTION DEBUG ===');
            console.log('Meter ID:', meterId);
            console.log('Tariff ID:', tariffId, 'Type:', typeof tariffId);
            console.log('All TOD Rules:', allTodRules.length);

            if (baseRate === 0) {
                showError('WARNING: Base rate is 0 for this meter. Please check tariff configuration.');
            }

            // Get tariff name for display
            const selectedMeter = allMeters.find(m => m.meterId == meterId);
            const tariffName = selectedMeter?.tariffName || 'Unknown';

            console.log('Tariff Name:', tariffName);

            document.getElementById('meterDetails').textContent =
                `Meter: #${meterId} | Consumer: ${consumerName} | Tariff: ${tariffName} | Base Rate: ₹${baseRate.toFixed(4)}/kWh | Latest Reading: ${currentMeterLatestReading.toFixed(2)} kWh`;
            document.getElementById('meterInfoDisplay').style.display = 'block';

            console.log('Filtering rules...');
            console.log('Before filter - All rules:', allTodRules.map(r => `ID:${r.todRuleId} TariffID:${r.tariffId} Name:${r.ruleName}`));

            currentMeterApplicableRules = allTodRules.filter(rule => {
                const ruleTariffId = parseInt(rule.tariffId);
                const meterTariffId = parseInt(tariffId);

                console.log(`Comparing rule tariffId ${ruleTariffId} === meter tariffId ${meterTariffId}: ${ruleTariffId === meterTariffId}`);
                return ruleTariffId === meterTariffId;
            });

            console.log('After filter - Applicable rules:', currentMeterApplicableRules.length);
            console.log('Filtered rules:', currentMeterApplicableRules.map(r => `${r.ruleName} (${r.startTime}-${r.endTime})`));
            console.log('=== END DEBUG ===');

            document.getElementById('todRulesCount').textContent = `${currentMeterApplicableRules.length} rules loaded for ${tariffName} tariff`;

            if (currentMeterApplicableRules.length === 0) {
                showError(`No TOD rules found for ${tariffName} tariff (ID: ${tariffId}). Please configure TOD rules for this tariff first.`);
                const tbody = document.getElementById('readingsTableBody');
                tbody.innerHTML = `<tr><td colspan="6" class="text-center text-warning py-4">
                    <i class="bi bi-exclamation-triangle display-6 d-block mb-2"></i>
                    No TOD rules configured for ${tariffName} tariff
                </td></tr>`;
                return;
            }

            checkExistingReadingsForDate();
        } else {
            document.getElementById('meterInfoDisplay').style.display = 'none';
            document.getElementById('todRulesCount').textContent = '0 rules loaded';
            currentMeterLatestReading = 0;
            currentMeterApplicableRules = [];

            const tbody = document.getElementById('readingsTableBody');
            tbody.innerHTML = '<tr><td colspan="6" class="text-center text-muted py-4"><i class="bi bi-inbox display-6 d-block mb-2"></i>Select a meter to load TOD slots</td></tr>';
        }
    }

    async function checkExistingReadingsForDate() {
        const meterId = document.getElementById('meterId').value;
        const readingDate = document.getElementById('readingDate').value;

        if (!meterId || !readingDate) return;

        const existingReadings = await getExistingReadings(meterId, readingDate);

        if (existingReadings.length > 0) {
            showError(`⚠️ Readings already exist for this meter on ${readingDate}. Loading existing data.`);
            loadExistingReadings(existingReadings);
        } else {
            hideError();
            loadApplicableTodSlots();
        }
    }

    async function getExistingReadings(meterId, readingDate) {
        try {
            const startDate = readingDate;
            const endDate = readingDate;
            const response = await fetch(
                `${DAILY_READING_API_URL}/ByDateRange?meterId=${meterId}&startDate=${startDate}&endDate=${endDate}`
            );

            if (response.ok) {
                return await response.json();
            }
            return [];
        } catch (error) {
            console.error('Error checking existing readings:', error);
            return [];
        }
    }

    function loadExistingReadings(existingReadings) {
        const tbody = document.getElementById('readingsTableBody');
        tbody.innerHTML = '';

        if (existingReadings.length === 0) {
            loadApplicableTodSlots();
            return;
        }

        // Group readings by TOD rule for easier processing
        const readingsByTodRule = {};
        existingReadings.forEach(reading => {
            readingsByTodRule[reading.todRuleId] = reading;
        });

        // Sort TOD rules by start time to ensure proper order
        const sortedRules = [...currentMeterApplicableRules].sort((a, b) => {
            return a.startTime.localeCompare(b.startTime);
        });

        sortedRules.forEach((rule, index) => {
            const existingReading = readingsByTodRule[rule.todRuleId];

            let previousReadingValue = '';
            let currentReadingValue = '';
            let consumptionValue = '';

            if (existingReading) {
                previousReadingValue = existingReading.previousReading.toFixed(2);
                currentReadingValue = existingReading.currentReading.toFixed(2);
                consumptionValue = existingReading.consumptionKwh.toFixed(2);
            } else if (index === 0) {
                previousReadingValue = currentMeterLatestReading.toFixed(2);
            }

            const row = `
                        <tr>
                            <td>
                                <input type="hidden" class="tod-rule-id" value="${rule.todRuleId}">
                                <span class="fw-semibold">${rule.ruleName}</span>
                                <br><small class="text-muted">Surge: ${rule.surgeChargePercent}% | Discount: ${rule.discountPercent}%</small>
                            </td>
                            <td>
                                <input type="time" class="form-control form-control-sm start-time" value="${rule.startTime}" required readonly>
                            </td>
                            <td>
                                <input type="time" class="form-control form-control-sm end-time" value="${rule.endTime}" required readonly>
                            </td>
                            <td>
                                <input type="number" step="0.01" class="form-control form-control-sm previous-reading"
                                    placeholder="0.00" value="${previousReadingValue}" onchange="updateConsumptionDisplay(this)" required>
                            </td>
                            <td>
                                <input type="number" step="0.01" class="form-control form-control-sm current-reading"
                                    placeholder="0.00" value="${currentReadingValue}" onchange="updateConsumptionDisplay(this)" required>
                            </td>
                            <td>
                                <span class="consumption-display">${consumptionValue ? consumptionValue + ' kWh' : '-'}</span>
                            </td>
                        </tr>
                    `;

            tbody.insertAdjacentHTML('beforeend', row);
        });
    }

    function loadApplicableTodSlots() {
        const tbody = document.getElementById('readingsTableBody');
        tbody.innerHTML = '';

        if (currentMeterApplicableRules.length === 0) {
            tbody.innerHTML = '<tr><td colspan="6" class="text-center text-muted py-4"><i class="bi bi-exclamation-triangle display-6 d-block mb-2"></i>No TOD rules configured for this meter\'s tariff</td></tr>';
            return;
        }

        // Sort TOD rules by start time to ensure proper order
        const sortedRules = [...currentMeterApplicableRules].sort((a, b) => {
            return a.startTime.localeCompare(b.startTime);
        });

        console.log('Loading TOD slots:', sortedRules.map(r => `${r.ruleName} (${r.startTime}-${r.endTime})`));

        sortedRules.forEach((rule, index) => {
            const previousReadingValue = index === 0 ? currentMeterLatestReading.toFixed(2) : '';

            const row = `
                        <tr>
                            <td>
                                <input type="hidden" class="tod-rule-id" value="${rule.todRuleId}">
                                <span class="fw-semibold">${rule.ruleName}</span>
                                <br><small class="text-muted">Surge: ${rule.surgeChargePercent}% | Discount: ${rule.discountPercent}%</small>
                            </td>
                            <td>
                                <input type="time" class="form-control form-control-sm start-time" value="${rule.startTime}" required readonly>
                            </td>
                            <td>
                                <input type="time" class="form-control form-control-sm end-time" value="${rule.endTime}" required readonly>
                            </td>
                            <td>
                                <input type="number" step="0.01" class="form-control form-control-sm previous-reading"
                                    placeholder="0.00" value="${previousReadingValue}" onchange="updateConsumptionDisplay(this)" required>
                            </td>
                            <td>
                                <input type="number" step="0.01" class="form-control form-control-sm current-reading"
                                    placeholder="0.00" onchange="updateConsumptionDisplay(this)" required>
                            </td>
                            <td>
                                <span class="consumption-display">-</span>
                            </td>
                        </tr>
                    `;

            tbody.insertAdjacentHTML('beforeend', row);
        });
    }

    function updateConsumptionDisplay(input) {
        const row = input.closest('tr');
        const previous = parseFloat(row.querySelector('.previous-reading').value) || 0;
        const current = parseFloat(row.querySelector('.current-reading').value) || 0;
        const consumption = current - previous;
        const consumptionDisplay = row.querySelector('.consumption-display');

        if (consumption < 0) {
            row.querySelector('.current-reading').classList.add('validation-error');
            consumptionDisplay.textContent = '-';
            consumptionDisplay.style.color = '#dc3545';
        } else {
            row.querySelector('.current-reading').classList.remove('validation-error');
            consumptionDisplay.textContent = consumption.toFixed(2) + ' kWh';
            consumptionDisplay.style.color = '#198754';

            // Update next row's previous reading
            const nextRow = row.nextElementSibling;
            if (nextRow && nextRow.querySelector('.previous-reading')) {
                nextRow.querySelector('.previous-reading').value = current.toFixed(2);
                updateConsumptionDisplay(nextRow.querySelector('.current-reading'));
            }
        }
    }

    async function saveReadings() {
        const meterId = document.getElementById('meterId').value;
        const readingDate = document.getElementById('readingDate').value;

        if (!meterId || !readingDate) {
            showError('Please select meter and date');
            return;
        }

        const tbody = document.getElementById('readingsTableBody');
        const rows = tbody.querySelectorAll('tr');

        if (rows.length === 0 || rows[0].cells[0]?.colSpan === 6) {
            showError('Please add at least one reading');
            return;
        }

        const readings = [];

        for (const row of rows) {
            const todRuleId = parseInt(row.querySelector('.tod-rule-id')?.value);
            const startTime = row.querySelector('.start-time')?.value;
            const endTime = row.querySelector('.end-time')?.value;
            const previousReading = parseFloat(row.querySelector('.previous-reading')?.value);
            const currentReading = parseFloat(row.querySelector('.current-reading')?.value);

            if (!todRuleId || !startTime || !endTime || isNaN(previousReading) || isNaN(currentReading)) {
                showError('Please fill in all fields for each reading row');
                console.log(todRuleId);
                console.log(startTime);
                console.log(endTime);
                return;
            }

            if (currentReading < previousReading) {
                showError('Current reading cannot be less than previous reading');
                return;
            }

            readings.push({
                todRuleId: todRuleId,
                startTime: startTime,
                endTime: endTime,
                previousReading: previousReading,
                currentReading: currentReading
            });
        }

        const bulkData = {
            meterId: parseInt(meterId),
            readingDate: readingDate,
            recordedBy: 'Admin',
            readings: readings
        };

        try {
            const response = await fetch(`${DAILY_READING_API_URL}/CreateBulk`, {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                },
                body: JSON.stringify(bulkData),
            });

            const result = await response.json();

            if (response.ok) {
                showSuccess(`Success! ${result.count} reading(s) saved.\nTotal Consumption: ${result.totalConsumption.toFixed(2)} kWh\nTotal Amount: ₹${result.totalAmount.toFixed(2)}`);
                clearForm();
                await fetchMeters();
            } else {
                if (result.errors) {
                    showError('Errors: ' + result.errors.join(', '));
                } else {
                    showError(`Failed: ${result.error || JSON.stringify(result)}`);
                }
            }
        } catch (error) {
            showError('Network error: ' + error.message);
        }
    }

    function clearForm() {
        document.getElementById('meterId').value = '';
        document.getElementById('readingDate').valueAsDate = new Date();
        document.getElementById('meterInfoDisplay').style.display = 'none';
        document.getElementById('todRulesCount').textContent = '0 rules loaded';
        currentMeterLatestReading = 0;
        currentMeterApplicableRules = [];

        const tbody = document.getElementById('readingsTableBody');
        tbody.innerHTML = '<tr><td colspan="6" class="text-center text-muted py-4"><i class="bi bi-inbox display-6 d-block mb-2"></i>Select a meter to load TOD slots</td></tr>';

        hideError();
        hideSuccess();
    }

    async function viewAllReadings() {
        const modal = new bootstrap.Modal(document.getElementById('viewReadingsModal'));
        modal.show();
        const tbody = document.getElementById('allReadingsTableBody');
        tbody.innerHTML = '<tr><td colspan="9" class="text-center text-muted py-4"><i class="bi bi-inbox display-6 d-block mb-2"></i>Select filters to view readings</td></tr>';
    }

    async function loadAllReadings() {
        const filterMeterId = document.getElementById('filterMeterId').value;
        const startDate = document.getElementById('filterStartDate').value;
        const endDate = document.getElementById('filterEndDate').value;

        if (!filterMeterId && !startDate && !endDate) {
            const tbody = document.getElementById('allReadingsTableBody');
            tbody.innerHTML = '<tr><td colspan="9" class="text-center text-muted py-4"><i class="bi bi-inbox display-6 d-block mb-2"></i>Select filters to view readings</td></tr>';
            return;
        }

        const tbody = document.getElementById('allReadingsTableBody');
        tbody.innerHTML = '<tr><td colspan="9" class="text-center py-4">Loading...</td></tr>';

        try {
            let url = '';

            if (filterMeterId && startDate && endDate) {
                url = `${DAILY_READING_API_URL}/ByDateRange?meterId=${filterMeterId}&startDate=${startDate}&endDate=${endDate}`;
            } else if (filterMeterId) {
                url = `${DAILY_READING_API_URL}/ByMeter/${filterMeterId}`;
            }

            if (!url) {
                tbody.innerHTML = '<tr><td colspan="9" class="text-center text-muted py-4">Please select a meter to view readings</td></tr>';
                return;
            }

            const response = await fetch(url);
            if (!response.ok) throw new Error('Failed to load readings');

            const readings = await response.json();

            if (readings.length === 0) {
                tbody.innerHTML = '<tr><td colspan="9" class="text-center text-muted py-4"><i class="bi bi-inbox display-6 d-block mb-2"></i>No readings found</td></tr>';
                return;
            }

            tbody.innerHTML = readings.map(r => `
                        <tr>
                            <td>${r.readingDate}</td>
                            <td>${r.meterId}</td>
                            <td>${r.todRuleName}</td>
                            <td>${r.startTime} - ${r.endTime}</td>
                            <td>${r.previousReading.toFixed(2)}</td>
                            <td>${r.currentReading.toFixed(2)}</td>
                            <td>${r.consumptionKwh.toFixed(2)} kWh</td>
                            <td>₹${r.amount.toFixed(2)}</td>
                            <td class="text-center">
                                <button class="btn btn-sm btn-outline-danger border-0" onclick="deleteReading(${r.readingId})">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    `).join('');
        } catch (error) {
            tbody.innerHTML = '<tr><td colspan="9" class="text-center text-danger py-4">Error loading readings</td></tr>';
        }
    }

    async function deleteReading(id) {
        if (!confirm('Are you sure you want to delete this reading?')) return;

        try {
            const response = await fetch(`${DAILY_READING_API_URL}/${id}`, {
                method: 'DELETE'
            });

            if (response.ok) {
                showSuccess('Reading deleted successfully');
                await loadAllReadings();
            } else {
                showError('Failed to delete reading');
            }
        } catch (error) {
            showError('Error deleting reading');
        }
    }

    function clearFilters() {
        document.getElementById('filterMeterId').value = '';
        document.getElementById('filterStartDate').value = '';
        document.getElementById('filterEndDate').value = '';
        const tbody = document.getElementById('allReadingsTableBody');
        tbody.innerHTML = '<tr><td colspan="9" class="text-center text-muted py-4"><i class="bi bi-inbox display-6 d-block mb-2"></i>Select filters to view readings</td></tr>';
    }

    function viewMonthlyBills() {
        window.location.href = '/Home/MonthlyBill';
    }
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>